@page "/"

<PageTitle>Index</PageTitle>

<p> Index page content is shown </p>

<Button Type="ButtonType.Button" Color="ButtonColor.Primary" @onclick="SaveEmployee">
    Test
</Button>

@code {

    [Inject] protected ToastService ToastService { get; set; } = default!;

    private void SaveEmployee()
    {
        try
        {
            // TODO: call the service/api to save the employee details            
            ToastService.Notify(new ToastMessage(ToastType.Success, $"Employee details saved successfully."));
        }
        catch (Exception ex)
        {
            // handle exception

            ToastService.Notify(new(ToastType.Danger, $"Error: {ex.Message}."));
        }
    }
}